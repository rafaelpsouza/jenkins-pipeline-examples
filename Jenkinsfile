pipeline {
    agent any
    // agent { docker { image 'openjdk-7-jdk-alpine' } }
    stages {
        stage('Test') {
            steps {
                // sh 'java --version'
                sh 'whoami'
            }
        }
    }
}